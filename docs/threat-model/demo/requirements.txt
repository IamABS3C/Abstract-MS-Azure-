# Abstract AI-SOC notebook — analyst workspace dependencies.
#
# The engine itself (pipeline.py, identities.py, abstract_client.py, hunts.py,
# enrichment.py, report.py) is pure stdlib and needs NONE of these — it runs
# anywhere with Python 3.9+. These power the interactive notebook: charts,
# graphs, tables, and the live Abstract MCP connection.
#
#   python3 -m venv .venv
#   source .venv/bin/activate        # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#   python -m ipykernel install --user --name abstract-soc --display-name "Abstract AI-SOC"
#   jupyter lab soc_notebook.ipynb

# notebook runtime
jupyterlab>=4.0
ipykernel>=6.29
nbformat>=5.9
nbconvert>=7.10

# visualization + tabular analysis
matplotlib>=3.7
networkx>=3.1
numpy>=1.24
pandas>=2.0

# interactive (browser-native) visuals — JS bundled inline so the notebook works offline
plotly>=5.20
pyvis>=0.3.2

# optional: pure code-free dashboard mode  ->  voila soc_notebook.ipynb
voila>=0.5

# roadmap: threat hunting (msticpy), detection-as-code (pySigma), ATT&CK (attackcti),
# anomaly detection (PyOD), interactive graphs (ipycytoscape) — all keyless
msticpy>=2.0
pysigma>=0.11
attackcti>=0.4
pyod>=1.1
ipycytoscape>=1.3

# in-notebook interactive control panel (GUI cockpit: connect / authorize / dev console)
ipywidgets>=8.1

# Model Context Protocol — connect the notebook to the Abstract MCP server
# (bundled at solution/mcp/abstract_mcp_server.py) and call its tools.
mcp>=1.2
